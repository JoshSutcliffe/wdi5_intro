<%= form_for @dish do |f| %>
  <!-- Using a tag doesnt need authentication helper, as it does it itself -->
  <label for="">Name</label>
  <%= f.text_field :name %>

  <button>Submit</button>
<% end %>